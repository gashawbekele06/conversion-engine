# Core runtime
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0
httpx>=0.27
requests>=2.31

# Data / enrichment
pandas>=2.2
numpy>=1.26
playwright>=1.42  # optional — used for live job-post crawl

# Integrations (optional — pipeline runs without them)
resend>=2.0
africastalking>=1.2

# LLM client
openai>=1.30    # also used as OpenRouter OpenAI-compatible client
anthropic>=0.25 # eval-tier fallback

# Observability
langfuse>=2.20

# τ²-Bench is pulled as git submodule; wrapper imports it conditionally
# tau2-bench

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
